export interface Users {
    userID: number;
    firstName: string;
    lastName: string;
    birthDate: Date;
    phoneNumber?: string;
    email: string;
    city?: string;
    password: string;
    //photo?
}
